name: Frontend Quality

on:
  pull_request:
    paths:
      - ".github/public-graph.lock.json"
      - ".github/scripts/**"
      - ".github/workflows/frontend-quality.yml"
      - ".gitignore"
      - "index.html"
      - "scripts/**"
      - "sitemap.xml"
  push:
    branches:
      - main
    paths:
      - ".github/public-graph.lock.json"
      - ".github/scripts/**"
      - ".github/workflows/frontend-quality.yml"
      - ".gitignore"
      - "index.html"
      - "scripts/**"
      - "sitemap.xml"
  workflow_dispatch:
  workflow_run:
    workflows:
      - "pages-build-deployment"
    types:
      - completed

permissions:
  contents: read

jobs:
  script-self-check:
    name: Script Self Check
    if: >
      github.event_name == 'pull_request' ||
      github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Verify public graph fetch failure fixture
        run: node .github/scripts/fetch_public_graph.mjs --self-test

      - name: Fetch pinned public graph
        run: node .github/scripts/fetch_public_graph.mjs

      - name: Synchronize generated homepage graph
        run: node scripts/sync_public_graph.mjs

      - name: Audit generated homepage graph
        run: node .github/scripts/check_frontend_public_graph.mjs

      - name: Run remaining repo-local audits
        run: |
          node .github/scripts/frontend_quality_fixtures.mjs
          node .github/scripts/check_frontend_quality.mjs --fixture-self-test

  live-quality-gate:
    name: Live Quality Gate
    # Run browser/live checks only after Pages deploys or by manual dispatch.
    # Push/PR events keep the repo-local self checks above, avoiding false failures while
    # GitHub Pages is still serving the previous deployment.
    if: >
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_sha || github.sha }}

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Install Chrome
        uses: browser-actions/setup-chrome@v1

      - name: Verify Chrome
        run: google-chrome --version

      - name: Run full quality gate
        run: node .github/scripts/check_frontend_quality.mjs

      - name: Locate latest report
        if: ${{ always() }}
        id: report
        run: |
          latest="$(ls -td .omx/reports/frontend-quality-* | head -1)"
          echo "path=$latest" >> "$GITHUB_OUTPUT"
          echo "### Frontend quality report" >> "$GITHUB_STEP_SUMMARY"
          echo "\`$latest\`" >> "$GITHUB_STEP_SUMMARY"
          echo >> "$GITHUB_STEP_SUMMARY"
          sed -n '1,120p' "$latest/summary.md" >> "$GITHUB_STEP_SUMMARY"

      - name: Upload quality report artifact
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: frontend-quality-report-${{ github.run_id }}
          path: ${{ steps.report.outputs.path }}
          if-no-files-found: error
